{
  "key": "roomops",
  "name": "RoomOps",
  "version": "0.1.0",
  "description": "Digitales Housekeeping und Zimmerstatus-Modul. Verwaltet den Reinigungsstatus aller Zimmer, koordiniert Housekeeping-Teams, Supervisor-Inspektionen und Rezeptionsabstimmung in Echtzeit.",
  "author": "smilch",
  "min_core_version": "0.1.0",
  "scope": "site",
  "capabilities": [
    "housekeeping",
    "room_status",
    "room_board",
    "status_history",
    "mobile_first"
  ],
  "deployment": {
    "type": "compose",
    "mode": "per_site",
    "service_name_template": "{site_slug}-roomops",
    "compose_file_template": "modules/roomops.compose.yml",
    "healthcheck_path": "/health"
  },
  "routes": {
    "frontend_template": "/sites/{site_slug}/modules/roomops",
    "api_template": "/api/sites/{site_slug}/modules/roomops"
  },
  "permissions": [
    "roomops.read",
    "roomops.operate",
    "roomops.inspect",
    "roomops.assign",
    "roomops.configure",
    "roomops.export"
  ],
  "config_schema": {
    "type": "object",
    "properties": {
      "default_status_on_checkout": {
        "type": "string",
        "enum": ["dirty", "cleaning"],
        "default": "dirty",
        "description": "Status eines Zimmers nach dem Checkout"
      },
      "inspection_required": {
        "type": "boolean",
        "default": true,
        "description": "Inspektion (roomops.inspect) erforderlich bevor Zimmer auf 'ready' gesetzt werden kann"
      },
      "cleaning_time_warning_minutes": {
        "type": "integer",
        "default": 30,
        "description": "Warnung nach wie vielen Minuten aktiver Reinigung"
      }
    }
  }
}
